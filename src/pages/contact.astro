---
import Layout from "../layouts/Layout.astro";
---
<Layout region="na" title="Contact">
  <section id="contact" class="py-16 lg:py-20">
    <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
      <!-- Header -->
      <div class="max-w-3xl">
        <h1 class="text-3xl font-semibold tracking-tight text-slate-900 sm:text-4xl">
          Contact
        </h1>
        <p class="mt-4 text-base text-slate-600">
          Tell us a bit about your building, portfolio, or project and what you&rsquo;re trying to
          decide. We&rsquo;ll get back to you with a simple next step&mdash;no spam, no hard sell.
        </p>
      </div>
      <div class="mt-10 grid gap-10 lg:grid-cols-[minmax(0,2fr)_minmax(0,1.3fr)]">
        <!-- Contact form -->
        <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8">
          <!-- Success message -->
          <div id="success-message" class="mb-6 hidden rounded-xl border border-emerald-200 bg-emerald-50 p-4">
            <div class="flex items-start gap-3">
              <svg class="mt-0.5 h-5 w-5 flex-shrink-0 text-emerald-600" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              <div>
                <h3 class="text-sm font-semibold text-emerald-800">Message sent!</h3>
                <p class="mt-1 text-sm text-emerald-700">Thanks for reaching out. We'll get back to you within one business day.</p>
              </div>
            </div>
          </div>
          <form
            action="https://api.web3forms.com/submit"
            method="post"
            class="space-y-6"
          >
            <!-- Web3Forms configuration -->
            <input type="hidden" name="access_key" value="8c6ab3a1-43b4-4110-8b1b-4a8bb804b90c" />
            <input type="hidden" name="subject" value="New Contact Form Submission - Alturascope" />
            <input type="hidden" name="redirect" value="https://alturascope.com/contact?success=true" />
            <!-- Name -->
            <div>
              <label for="name" class="block text-sm font-medium text-slate-800">
                Name
              </label>
              <input
                type="text"
                id="name"
                name="name"
                autocomplete="name"
                required
                class="mt-2 block w-full rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm text-slate-900 shadow-sm outline-none ring-0 placeholder:text-slate-400 focus:border-slate-900 focus:ring-1 focus:ring-slate-900"
                placeholder="Your name"
              />
            </div>
            <!-- Email -->
            <div>
              <label for="email" class="block text-sm font-medium text-slate-800">
                Email address
              </label>
              <input
                type="email"
                id="email"
                name="email"
                autocomplete="email"
                required
                class="mt-2 block w-full rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm text-slate-900 shadow-sm outline-none ring-0 placeholder:text-slate-400 focus:border-slate-900 focus:ring-1 focus:ring-slate-900"
                placeholder="you@company.com"
              />
            </div>
            <!-- Company -->
            <div>
              <label for="company" class="block text-sm font-medium text-slate-800">
                Company name
              </label>
              <input
                type="text"
                id="company"
                name="company"
                class="mt-2 block w-full rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm text-slate-900 shadow-sm outline-none ring-0 placeholder:text-slate-400 focus:border-slate-900 focus:ring-1 focus:ring-slate-900"
                placeholder="Your organisation"
              />
            </div>
            <!-- Message -->
            <div>
              <label for="message" class="block text-sm font-medium text-slate-800">
                Message
              </label>
              <textarea
                id="message"
                name="message"
                rows="5"
                required
                class="mt-2 block w-full rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm text-slate-900 shadow-sm outline-none ring-0 placeholder:text-slate-400 focus:border-slate-900 focus:ring-1 focus:ring-slate-900"
                placeholder="Tell us about your site, portfolio, or the decision you’re trying to make."
              ></textarea>
            </div>
            <!-- Submit -->
            <div class="pt-2">
              <button
                type="submit"
                class="inline-flex w-full items-center justify-center rounded-full bg-slate-900 px-5 py-2.5 text-sm font-medium text-white shadow-sm hover:bg-slate-800 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-slate-900 focus-visible:ring-offset-2 focus-visible:ring-offset-white sm:w-auto"
              >
                Send message
              </button>
            </div>
          </form>
        </div>
        <!-- Side info panel -->
        <aside class="space-y-6 text-sm text-slate-700">
          <div class="rounded-2xl border border-slate-200 bg-slate-50 p-6">
            <h2 class="text-sm font-semibold uppercase tracking-wide text-slate-700">
              What to include
            </h2>
            <ul class="mt-3 list-disc space-y-1 pl-5">
              <li>City / market and approximate building size</li>
              <li>Whether it&rsquo;s As-Built, Progress, Diligence, or Stakeholder Day</li>
              <li>Your rough timing (this month, next 30&ndash;60 days, etc.)</li>
              <li>Any key risks or questions you already know about</li>
            </ul>
          </div>
          <div class="rounded-2xl border border-slate-200 bg-white p-6">
            <h2 class="text-sm font-semibold uppercase tracking-wide text-slate-700">
              Response time
            </h2>
            <p class="mt-2">
              We aim to respond to all enquiries within
              <span class="font-medium">one business day</span>. If it&rsquo;s urgent, mention your
              deadline in the message and we&rsquo;ll do our best to prioritise it.
            </p>
          </div>
        </aside>
      </div>
    </div>
  </section>
</Layout>

<script>
  // Show success message if redirected after form submission
  if (new URLSearchParams(window.location.search).get('success') === 'true') {
    document.getElementById('success-message')?.classList.remove('hidden');
    // Clean up URL without refreshing
    window.history.replaceState({}, '', window.location.pathname);
  }
</script>
